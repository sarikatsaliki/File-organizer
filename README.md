# File Flow Optimizer 🚀
A Python-based automation tool to organize messy directories into categorized folders.

## Features
- Scans directories for specific file extensions.
- Automatically creates category folders (Images, Docs, etc.).
- Prevents file overwriting with existence checks.

## Tech Stack
- Python 3.x
- `os` and `shutil` libraries